// This file is in charge of getting requested data from the database.

export default class FetchData {
    constructor() {

    }

    fetchCategories() {
        //returns an array of smaller categories
    }

    fetchSuperCategories() {
        //returns an array of main categories
    }

    fetchRecipes() {
        //returns an array of recipes in a category
    }

    fetchRecipe() {
        //Returns one recipe
    }

    fetchSubrecipes() {
        //Returns an array of subrecipes
    }
}
